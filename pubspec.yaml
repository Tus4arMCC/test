name: myapp
description: "A new Flutter project."
version: 1.0.0+1
environment:
  sdk: ^3.10.3
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  flutter_dotenv: ^6.0.0
  hive: ^2.0.5
  hive_flutter: ^1.1.0
  shared_preferences: ^2.5.4
  uuid: ^4.0.0
  qr_flutter: ^4.1.0
  share_plus: ^12.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^2.1.2
  hive_generator: ^1.1.1
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - lib/assets/1half.png
    - lib/assets/2half.png
